function isSameLength(word1, word2){
    if((word1.length)==(word2.length))
    {
        alert(true)
    }
    else{
        alert(false)
    }
}
    word1=prompt("enter the word1")
    word2=prompt("enter the word2")
    isSameLength(word1, word2)